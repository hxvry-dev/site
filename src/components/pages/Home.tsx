import { Container, Text } from '@mantine/core';
import XPCard from '../XPCard';

const Home = () => {
	return (
		<Container>
			<Text size="xl">Hello World</Text>
			<XPCard />
		</Container>
	);
};

export default Home;
