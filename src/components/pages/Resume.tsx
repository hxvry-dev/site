import { Container, Title } from '@mantine/core';
import JobExperienceCard from '../JobExperienceCard';

const Resume = () => {
	return (
		<Container>
			<Title>Jobs I've Had</Title>
			<JobExperienceCard />
		</Container>
	);
};

export default Resume;
