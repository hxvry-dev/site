import { createTheme } from '@mantine/core';

export const theme = createTheme({
	focusRing: 'never',
	fontSmoothing: true,
	colors: {},
	primaryShade: {
		light: 4,
		dark: 7,
	},
	autoContrast: true,
});
